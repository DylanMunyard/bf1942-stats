# SMTP Configuration (optional, if you want email alerts in the future)
# [smtp]
# enabled = true
# host = smtp.gmail.com:587
# user = ${GF_SMTP_USER}
# password = ${GF_SMTP_PASSWORD}
# from_address = ${GF_SMTP_FROM_ADDRESS}
# from_name = BF1942 Stats Alerts
# skip_verify = false
# startTLS_policy = MandatoryStartTLS

[alerting]
enabled = true
execute_alerts = true

[unified_alerting]
enabled = true
# Default evaluation interval
execute_alerts = true
evaluation_timeout = 30s
max_attempts = 3

[server]
root_url = http://localhost:3000
